mixin list-transactions(title, items, colorClass, path)
  .card.card-custom.mt-4
    .card-body
      h5.card-title.mb-3= title
      
      if items.length > 0
        .table-responsive
          table.table.table-dark.table-hover.align-middle
            thead
              tr
                th STT
                th Ngày
                th Danh mục
                th Mô tả
                th.text-end Số tiền
                th.text-end Hành động
            tbody
              each item, index in items
                tr
                  td= index + 1
                  td= item.date.toLocaleDateString('vi-VN')
                  td 
                    span.badge.rounded-pill(class=colorClass)= item.category
                  td= item.description
                  td.text-end.fw-bold= item.amount.toLocaleString('vi-VN') + ' ₫'
                  td.text-end
                    form(
                      action=`/${path}/delete/${item.id}`
                      method="POST"
                      style="display: inline-block;"
                      onsubmit="return confirm('Bạn có chắc chắn muốn xóa không?')"
                    )
                      button.btn.btn-outline-danger.btn-sm(type="submit") 🗑️ Xóa
      else
        p.text-center.text-muted.fst-italic Chưa có dữ liệu nào.